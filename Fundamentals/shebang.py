#!/usr/bin/python3

# O comentário acima seta o interpretador que irá
# executar o arquivo. Assim, não precisamos utilizar o
# comando python no console, bastando apenas passar o
# caminho relativo do arquivo (ex: ./shebang.py).
